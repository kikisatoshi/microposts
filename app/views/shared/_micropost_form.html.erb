<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t('view.Compose new micropost') %>
  </div>
  <%= f.submit t('view.Post'), class: "btn btn-primary" %>
<% end %>
